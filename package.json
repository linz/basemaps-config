{
  "name": "basemaps-config",
  "version": "1.0.1",
  "description": "LINZ specific configuration and deployment of basemaps",
  "repository": "github:linz/basemaps-config",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "lint": "prettier config/**/*.json --check --parser=json-stringify"
  },
  "devDependencies": {
    "@linzjs/style": "^5.0.0",
    "prettier-plugin-sort-json": "^3.0.0"
  }
}
